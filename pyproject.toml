[project]
name = "aismartfolder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.64.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.4",
]
